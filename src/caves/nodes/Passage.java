package caves.nodes;

import java.util.Date;
import java.util.Random;
import com.google.common.collect.HashBiMap;

public class Passage extends Location {
	/*static Random r = new Random(new Date().getTime());
	public static HashBiMap<Exit, Exit> passages = HashBiMap.create();
	//public static ArrayList<Passage> passages = new ArrayList<Passage>();
	Exit[] nodes = new Exit[2];
	public Passage() {
		this(new Exit());
	}
	public Passage(Exit n1) {
		this(n1, Exit.deadEnd);
	}
	public Passage(Exit n1, Exit n2) {
		nodes[0] = n1;
		nodes[1] = n2;
		passages.put(n1, n2);
	}*/
}
